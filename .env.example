# Database Configuration
DB_HOST=localhost
DB_PORT=27017
DB_NAME=research_papers

# Redis Configuration
REDIS_CACHE=localhost
REDIS_PORT=6379
REDIS_DB=0

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True

# Optional: MongoDB Authentication (if using authentication)
# DB_USERNAME=your_username
# DB_PASSWORD=your_password

# Optional: Redis Authentication (if using authentication)
# REDIS_PASSWORD=your_redis_password